spring:
  profiles:
    active: dev
  cloud:
    config:
      label: master
      name: configclient
      discovery:
        enabled: true
        service-id: configserver1
#eureka:
#  client:
#    service-url:
#      defaultZone: http://localhost:8761/eureka/